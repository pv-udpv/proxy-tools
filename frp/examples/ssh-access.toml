# Expose local SSH service through frp
serverAddr = "frps.example.com"
serverPort = 7000
auth.method = "token"
auth.token = "change-this-secret-token"

[[proxies]]
name = "ssh"
type = "tcp"
localIP = "127.0.0.1"
localPort = 22
remotePort = 6022   # connect via: ssh -p 6022 user@frps.example.com
